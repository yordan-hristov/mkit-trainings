import styled from "styled-components";

/**
 * Exercise #1 - Toggle Button
 *
 * @param {boolean} isOn
 * @returns {React.ReactElement}
 */
export function Toggle({ isOn }) {
  // Implement it

  return <div>Toggle</div>;
}

/**
 * Exercise #2 - Colorful Word
 *
 * @param {string} word
 * @returns {React.ReactElement}
 */
export function ColorfulWord({ word }) {
  // Implement it

  return <div>ColorfulWord</div>;
}

/**
 * Exercise #3 - Fully Controlled Styled Component
 *
 * @param {CSSObject} styles
 * @returns {React.ReactElement}
 */
export function StyledComponent({ styles }) {
  // Implement it

  return <div>StyledComponent</div>;
}

/**
 * Exercise #4 - Styled Children
 *
 * @param {React.Node} children
 * @returns {React.ReactElement}
 */
export function StyledChildren({ children }) {
  // Implement it

  return <div>StyledChildren</div>;
}

/**
 * Exercise #5 - Responsive Box
 *
 * @returns {React.ReactElement}
 */
export function Box() {
  // Implement it

  return <div>Box</div>;
}
