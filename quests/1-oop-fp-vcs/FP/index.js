// Assessment 1

const map = (array) => {};

const filter = (array) => {};

const reduce = (array) => {};

// Assessment 2

const sort = (array) => {};

// Assessment 3

const calculator = {};

// Assessment 4

function recursive(n, result = []) {}

// Assessment 5

function createFibonacciClosure() {}

// Assessment 6

function add(n) {}

module.exports = {
  map,
  filter,
  reduce,
  sort,
  calculator,
  recursive,
  createFibonacciClosure,
  add,
};
